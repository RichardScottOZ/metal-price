currency: (https://exchangeratesapi.io)
    GetRate( base, destination ) return ( rate )
    base, destination = enum CZK, USD, EUR ...

metal: (https://www.moneymetals.com/api/spot-prices.json)
    GetValue ( metal ) return ( repeated value ) // USD, oz
    metal = enum silver, copper, gold ...
    message Value {
        int amount
        string unit
    }

API:
    http://host:3000/api/v1/palladium/czk
    1. []v, b = GetValue( "Pd" )
    2. r = GetRate( b, "CZK" )
    3. []value = v * r
    4. return []value
